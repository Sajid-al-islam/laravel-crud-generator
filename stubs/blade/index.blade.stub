@extends('{{ layout }}')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">{{ modelName }} Management</h4>
                    <a href="{{ route('{{ routePrefix }}.create') }}" class="btn btn-primary">
                        <i class="fas fa-plus me-1"></i>Add New {{ modelName }}
                    </a>
                </div>

                <div class="card-body">
                    @if (session('success'))
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle me-2"></i>{{ session('success') }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    @endif

                    <div class="row mb-3">
                        <div class="col-md-8">
                            <form action="{{ route('{{ routePrefix }}.index') }}" method="GET" class="d-flex gap-2">
                                <input type="search" name="search" class="form-control"
                                    placeholder="Search {{ modelNameLowerPlural }}..."
                                    value="{{ request('search') }}">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-search"></i>
                                </button>
                            </form>
                        </div>
                    </div>

                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
{{ tableHeaders }}
                                    <th width="200">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @forelse(${{ modelPluralVariable }} as ${{ modelVariable }})
                                    <tr>
{{ tableData }}
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a href="{{ route('{{ routePrefix }}.show', ${{ modelVariable }}) }}" class="btn btn-info btn-sm" title="View">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a href="{{ route('{{ routePrefix }}.edit', ${{ modelVariable }}) }}" class="btn btn-warning btn-sm" title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <form action="{{ route('{{ routePrefix }}.destroy', ${{ modelVariable }}) }}" method="POST" style="display: inline;">
                                                    @csrf
                                                    @method('DELETE')
                                                    <button type="submit" class="btn btn-danger btn-sm"
                                                        onclick="return confirm('Are you sure?')" title="Delete">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                @empty
                                    <tr>
                                        <td colspan="100" class="text-center py-4">
                                            <p class="text-muted mb-0">No {{ modelNameLowerPlural }} found.</p>
                                        </td>
                                    </tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mt-3">
                        <div class="text-muted">
                            Showing {{ ${{ modelPluralVariable }}->firstItem() ?? 0 }} to {{ ${{ modelPluralVariable }}->lastItem() ?? 0 }}
                            of {{ ${{ modelPluralVariable }}->total() }} entries
                        </div>
                        <div>{{ ${{ modelPluralVariable }}->links() }}</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
